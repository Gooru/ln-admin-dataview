<div class="panel-tabs">
  <ul class="competencies-header">
    <li class="{{if isCompetency 'tab-active' ''}}" {{action 'competencyMatrixTabs' 'competency' }}>
      <span> Comptency  </span> </li>
    <li class="{{if isJourney 'tab-active' ''}}" {{action 'competencyMatrixTabs' 'journey' }}>
      <span>Journeys </span> </li>
  </ul>

  {{#if isCompetency}}
    {{learner.competencies-journeys-list taxonomySubjects=taxonomySubjects selectedSubject='selectedSubject'}}
  {{else}}
    {{learner.competencies-journeys-list   selectedSubject='selectedSubject'}}
  {{/if}}
</div>

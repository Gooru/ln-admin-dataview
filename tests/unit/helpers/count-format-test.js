
import { countFormat } from 'admin-dataview/helpers/count-format';
import { module, test } from 'qunit';

module('Unit | Helper | count format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = countFormat([42]);
  assert.ok(result);
});



import { splitText } from 'admin-dataview/helpers/split-text';
import { module, test } from 'qunit';

module('Unit | Helper | split text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = splitText([42]);
  assert.ok(result);
});


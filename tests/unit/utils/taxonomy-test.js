import taxonomy from 'admin-dataview/utils/taxonomy';
import { module, test } from 'qunit';

module('Unit | Utility | taxonomy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taxonomy();
  assert.ok(result);
});
